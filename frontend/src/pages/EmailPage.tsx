import React from 'react';
import EmailDetailPage from '../components/EmailDetail';

const EmailPage: React.FC = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <EmailDetailPage />
    </div>
  );
};

export default EmailPage;
